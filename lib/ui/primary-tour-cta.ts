/**
 * Extra classes for `<Button variant="primary">` (or `asChild` + `Link`) so primary CTAs
 * match the tour sidebar “Check dates & book” control: padding, type scale, tracking.
 */
export const primaryTourCtaClassName =
  "rounded-sm h-auto min-h-0 border-0 py-4 md:py-5 text-lg md:text-xl font-bold tracking-tighter";
